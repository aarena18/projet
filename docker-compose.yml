services:
  postgres:
    image: postgres:15
    container_name: serverless_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: serverless
      POSTGRES_DB: serverless_projet
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
