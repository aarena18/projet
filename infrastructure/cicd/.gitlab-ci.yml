serverless-projet-assets:
  stage: deploy
  image: python:3.10
  variables:
    GIT_STRATEGY: fetch
  before_script:
    - apt-get update -y
    - apt-get install -y npm
    - python -m pip install --upgrade pip
    - pip install boto3 python-dotenv
    - node --version
    - npm --version
    - python --version
  script:
    - python $CI_PROJECT_DIR/infrastructure/scripts/deploy.folder.py assets
  rules:
    - if: '$CI_COMMIT_BRANCH == "stg"'
    - if: '$CI_COMMIT_BRANCH == "main"'

serverless-projet-stg:
  stage: deploy
  image: python:3.10
  variables:
    GIT_STRATEGY: fetch
  before_script:
    - apt-get update -y
    - apt-get install -y npm
    - python -m pip install --upgrade pip
    - pip install boto3 python-dotenv
    - node --version
    - npm --version
    - python --version
  script:
    - cd code/api/my-app
    - npm install
    - python $CI_PROJECT_DIR/infrastructure/scripts/deploy.front.py serverless_projet_stg
  rules:
    - if: '$CI_COMMIT_BRANCH == "stg"'
  environment:
    name: stg
  after_script:
    - echo "Déploiement STG USER terminé"

serverless-projet-prd:
  stage: deploy
  image: python:3.10
  variables:
    GIT_STRATEGY: fetch
  before_script:
    - apt-get update -y
    - apt-get install -y npm
    - python -m pip install --upgrade pip
    - pip install boto3 python-dotenv
    - node --version
    - npm --version
    - python --version
  script:
    - cd code/api/my-app
    - npm install
    - python $CI_PROJECT_DIR/infrastructure/scripts/deploy.front.py serverless_projet_prd
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  environment:
    name: prd
  after_script:
    - echo "Déploiement PRD USER terminé"

serverless-projet-admin-stg:
  stage: deploy
  image: python:3.10
  variables:
    GIT_STRATEGY: fetch
  before_script:
    - apt-get update -y
    - apt-get install -y npm
    - python -m pip install --upgrade pip
    - pip install boto3 python-dotenv
    - node --version
    - npm --version
    - python --version
  script:
    - cd code/api/my-app
    - npm install
    - python $CI_PROJECT_DIR/infrastructure/scripts/deploy.front.py serverless_projet_admin_stg
  rules:
    - if: '$CI_COMMIT_BRANCH == "stg"'
  environment:
    name: stg
  after_script:
    - echo "Déploiement STG ADMIN terminé"

serverless-projet-admin-prd:
  stage: deploy
  image: python:3.10
  variables:
    GIT_STRATEGY: fetch
  before_script:
    - apt-get update -y
    - apt-get install -y npm
    - python -m pip install --upgrade pip
    - pip install boto3 python-dotenv
    - node --version
    - npm --version
    - python --version
  script:
    - cd code/api/my-app
    - npm install
    - python $CI_PROJECT_DIR/infrastructure/scripts/deploy.front.py serverless_projet_admin_prd
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  environment:
    name: prd
  after_script:
    - echo "Déploiement PRD ADMIN terminé"
